#include "main.h"
#include <stdio.h>

/**
 * _abs - funtion that computes the value of an integer
 *
 * Return Always 0
 */

 int _abs(int);
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
return (0);
}
